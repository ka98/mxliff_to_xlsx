# mxliff_to_xlsx
A python script that converts a Phrase TMS MXLIFF File to an Excel Spreadsheet

## Usage
this script expects a `input.mxliff` in the currently working directory and will export the file to a `output.xlsx` file.

This scripts tries to keep the formatting, if there are any issues please file a bug report.